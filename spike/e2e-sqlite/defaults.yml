# sqlite e2e: schedule every minute so the daemon performs the dump; local storage.
default:
  schedule:
    cron: "* * * * *"
  compress_with:
    type: tgz
  default_storage: local
  storages:
    local:
      type: local
      path: /backups/{{ .Model }}
      keep: 3
