# e2e profile: compress + local storage under /backups/<model>.
default:
  compress_with:
    type: tgz
  default_storage: local
  storages:
    local:
      type: local
      path: /backups/{{ .Model }}
      keep: 5
